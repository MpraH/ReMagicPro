using System.Collections.Generic;

public abstract class Effect
{
    /*public string description;
    public List<dynamic> targets; // also includes cards and players that are not "targeted" like effects that affect all creates

    public void Resolve()
    {
        foreach (dynamic target in targets)
        {
            ResolveForTarget(target);
        }
    }
    protected abstract void ResolveForTarget(dynamic target);
    */
}